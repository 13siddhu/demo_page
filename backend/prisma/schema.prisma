generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model DemoRequest {
  id        Int      @id @default(autoincrement())
  name      String
  email     String
  phone     String
  interest  String
  consent   Boolean
  createdAt DateTime @default(now())
}
